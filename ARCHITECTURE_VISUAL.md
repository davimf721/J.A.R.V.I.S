# 🎭 JARVIS Microservices - Resumo Visual da Arquitetura

## 📊 Diagrama da Arquitetura Completa

```
╔═══════════════════════════════════════════════════════════════════════════╗
║                         JARVIS AI PLATFORM v1.0                           ║
║                  Arquitetura de Microserviços Containerizada              ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│                            CAMADA DE CLIENTE                             │
├─────────────────────────────────────────────────────────────────────────┤
│  CLI (Terminal)  │  Web App (Future)  │  Mobile App (Future)            │
└────────────────────────────┬──────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│               ORQUESTRADOR (API GATEWAY) - PORT 8010                    │
├─────────────────────────────────────────────────────────────────────────┤
│  • Recebe requisições de podcast                                        │
│  • Orquestra 5 microserviços em sequência                               │
│  • Gerencia jobs assincronamente                                        │
│  • Cacheia resultados                                                   │
│  • FastAPI + Redis + Background Tasks                                   │
└────────────────────────────┬──────────────────────────────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
   ┌─────────────┐    ┌──────────────┐   ┌─────────────┐
   │LLM SERVICE  │    │NEWS SERVICE  │   │MEMORY SERV  │
   │  PORT 8001  │    │  PORT 8002   │   │  PORT 8005  │
   └─────────────┘    └──────────────┘   └─────────────┘
        │ Ollama           │ RSS Feeds         │ ChromaDB
        │ (kimi-k2.5)      │ (8 sources)       │ (Vector)
        │                  │                   │
        └────────────┬─────────────────────────┘
                     │
                     ▼
            ┌──────────────────────┐
            │ SCRIPT SERVICE       │
            │ PORT 8003            │
            └──────────┬───────────┘
                       │ Generates podcast script
                       │ (800-1200 words)
                       │
                       ▼
            ┌──────────────────────┐
            │ TTS SERVICE          │
            │ PORT 8004            │
            └──────────┬───────────┘
                       │ edge-tts
                       │ Generates MP3
                       │ 8-12 minutes
                       │
                       ▼
            ┌──────────────────────┐
            │ STORAGE (Minio)      │
            │ PORT 9000            │
            └──────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                    INFRAESTRUTURA DE DADOS                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌──────────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐       │
│  │ PostgreSQL   │  │  Redis   │  │RabbitMQ  │  │  ChromaDB    │       │
│  │ PORT 5432    │  │ 6379     │  │  5672    │  │  PORT 8200   │       │
│  │ ┌──────────┐ │  │ Cache    │  │ Queues   │  │ Vector DB    │       │
│  │ │ Users    │ │  │ - LLM    │  │ - Jobs   │  │ - Memories   │       │
│  │ │ Agents   │ │  │ - News   │  │ - TTS    │  │ - Embeddings │       │
│  │ │ Jobs     │ │  │ - Script │  │ - Email  │  │              │       │
│  │ │ Results  │ │  │ 4-24h    │  │          │  │              │       │
│  │ └──────────┘ │  │          │  │          │  │              │       │
│  └──────────────┘  └──────────┘  └──────────┘  └──────────────┘       │
│                                                                          │
│  ┌──────────────────────────┐      ┌──────────────────────────┐       │
│  │      OLLAMA (LLM)        │      │   MINIO (Storage)        │       │
│  │      PORT 11435          │      │   PORT 9000/9001         │       │
│  │ ┌──────────────────────┐ │      │ ┌──────────────────────┐ │       │
│  │ │ Modelo: kimi-k2.5    │ │      │ │ Bucket: jarvis-media │ │       │
│  │ │ (Quantizado - 7B)    │ │      │ │ - Podcasts (MP3)     │ │       │
│  │ │ CPU/GPU compatible   │ │      │ │ - Logs               │ │       │
│  │ │ Contexto: 8192 tokens│ │      │ │ - Backups            │ │       │
│  │ └──────────────────────┘ │      │ └──────────────────────┘ │       │
│  └──────────────────────────┘      └──────────────────────────┘       │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                    MONITORAMENTO & OBSERVABILIDADE                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌──────────────────┐              ┌──────────────────┐                │
│  │   PROMETHEUS     │              │     GRAFANA      │                │
│  │   PORT 9090      │─────────────▶│   PORT 3000      │                │
│  │                  │              │   (Dashboards)   │                │
│  │ • Métricas       │              │ • Visualização   │                │
│  │ • Time-series DB │              │ • Alertas        │                │
│  │ • Scrape config  │              │ • User/pass      │                │
│  └──────────────────┘              └──────────────────┘                │
│                                                                          │
│  Coleta dados de:                                                      │
│  - Uso de CPU/Memória por container                                   │
│  - Requisições HTTP (rate, latência)                                  │
│  - Job duration (tempo pipeline)                                      │
│  - Cache hit/miss ratio                                               │
│  - Database connections                                               │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

## 🔄 Pipeline de Execução (Passo a Passo)

```
1. REQUISIÇÃO CHEGA
   ┌──────────────────────────────────────────┐
   │ POST /api/podcast/generate               │
   │ {                                        │
   │   "agent_name": "jarvis",               │
   │   "agent_type": "podcast_daily",        │
   │   "user_id": "user_123",                │
   │   "news_count": 8                       │
   │ }                                        │
   └──────────────────┬───────────────────────┘
                      │
                      ▼ (IMEDIATAMENTE RETORNA job_id)
   ┌──────────────────────────────────────┐
   │ job_id: abc-123-def                   │
   │ status: "pending"                     │
   └──────────────────┬───────────────────┘
                      │
                      ▼ (INICIA BACKGROUND PROCESSING)

2. ORQUESTRADOR INICIA PIPELINE
   ┌─────────────────────────────────────────┐
   │ Step 1: Buscar Notícias                 │
   │ POST /api/news/fetch                    │
   │ Response: [news1, news2, ..., news8]    │
   │ Tempo: ~2 segundos                      │
   └─────────────────┬───────────────────────┘
                     │
                     ▼
   ┌─────────────────────────────────────────┐
   │ Step 2: Recuperar Memória Anterior      │
   │ POST /api/memory/recall                 │
   │ Response: [memory1, memory2, ...]       │
   │ Tempo: ~0.5 segundos                    │
   └─────────────────┬───────────────────────┘
                     │
                     ▼
   ┌─────────────────────────────────────────┐
   │ Step 3: Gerar Roteiro                   │
   │ POST /api/script/generate               │
   │ - Combina notícias + memória            │
   │ - Chama LLM Service                     │
   │ - LLM chama Ollama (kimi-k2.5)         │
   │ Response: "E aí, Davi. Domingo..."     │
   │ Tempo: ~3-5 minutos (modelo)            │
   └─────────────────┬───────────────────────┘
                     │
                     ▼
   ┌─────────────────────────────────────────┐
   │ Step 4: Gerar Áudio (TTS)               │
   │ POST /api/tts/generate                  │
   │ - Converte texto em fala                │
   │ - Usa edge-tts                          │
   │ - Salva em Minio                        │
   │ Response: audio_path, duration          │
   │ Tempo: ~1-2 minutos                     │
   └─────────────────┬───────────────────────┘
                     │
                     ▼
   ┌─────────────────────────────────────────┐
   │ Step 5: Armazenar Memória               │
   │ POST /api/memory/store                  │
   │ - Salva aprendizado para próx podcast  │
   │ Tempo: ~0.2 segundos                    │
   └─────────────────┬───────────────────────┘
                     │
                     ▼
   ┌─────────────────────────────────────────┐
   │ ✅ PIPELINE CONCLUÍDO                   │
   │ status: "completed"                     │
   │ Tempo total: ~5-7 minutos               │
   └─────────────────────────────────────────┘

3. USUÁRIO RECUPERA RESULTADO
   ┌──────────────────────────────────────────┐
   │ GET /api/podcast/result/abc-123-def      │
   │                                          │
   │ Response:                                │
   │ {                                        │
   │   "script": "E aí, Davi...",           │
   │   "audio_path": "/tmp/tts_output/...", │
   │   "audio_duration": 598.5,              │
   │   "news_used": [8 items],               │
   │   "memory_recalled": "...",             │
   │   "status": "completed"                 │
   │ }                                        │
   └──────────────────────────────────────────┘
```

## 📈 Fluxo de Dados Entre Serviços

```
┌──────────────┐
│   CLIENT     │
└──────┬───────┘
       │ POST /api/podcast/generate
       ▼
┌──────────────────────────────────────────────────┐
│         ORCHESTRATOR (FastAPI)                   │
│  - Cria job em memória                           │
│  - Retorna job_id imediatamente                  │
│  - Inicia processamento background               │
└──────┬───────────────────────────────────────────┘
       │
       ├──────────┬──────────┬──────────┐
       │          │          │          │
       ▼          ▼          ▼          ▼
   ┌────────┐ ┌────────┐ ┌──────────┐ ┌──────────┐
   │ NEWS   │ │ MEMORY │ │SCRIPT    │ │ TTS      │
   │Service │ │Service │ │ Service  │ │ Service  │
   │ 8002   │ │ 8005   │ │ 8003     │ │ 8004     │
   └────────┘ └────────┘ └──────────┘ └──────────┘
       │          │          │            │
       │ RSS      │ Vector   │ Ollama    │ edge-tts
       │ Feeds    │ Search   │ Call      │ API
       │          │          │            │
       ▼          ▼          ▼            ▼
   ┌────────┐ ┌────────┐ ┌──────────┐ ┌──────────┐
   │NewsDB  │ │ChromaDB│ │ OLLAMA   │ │ EDGE-TTS │
   │ Cache  │ │(Redis) │ │ 11435    │ │ Cloud    │
   └────────┘ └────────┘ └──────────┘ └──────────┘
       │          │          │            │
       │ Tópicos  │ Contexto │ Resposta  │ Áudio MP3
       │          │          │            │
       └──────────┴──────────┴────────────┘
              │
              ▼
        ┌────────────────────┐
        │ MINIO (S3)         │
        │ Armazena:          │
        │ - MP3 final        │
        │ - Metadata         │
        │ - Logs             │
        └────────────────────┘
```

## 🎯 Matriz de Responsabilidades

```
┌─────────────────┬──────────┬──────────┬──────────┬──────────┬──────────┐
│ Responsabilidade│ Orch.    │ LLM      │ News     │ Script   │ TTS      │
├─────────────────┼──────────┼──────────┼──────────┼──────────┼──────────┤
│ Coordenação     │    ✓     │          │          │          │          │
│ Busca Notícias  │          │          │    ✓     │          │          │
│ Geração Texto   │          │    ✓     │          │    ✓     │          │
│ Síntese Voz     │          │          │          │          │    ✓     │
│ Armazenamento   │          │          │          │          │    ✓     │
│ Cache           │    ✓     │    ✓     │    ✓     │    ✓     │    ✓     │
│ Logging         │    ✓     │    ✓     │    ✓     │    ✓     │    ✓     │
│ Health Check    │    ✓     │    ✓     │    ✓     │    ✓     │    ✓     │
└─────────────────┴──────────┴──────────┴──────────┴──────────┴──────────┘
```

## 📦 Stack Tecnológico

| Componente | Tecnologia | Versão | Razão |
|-----------|-----------|--------|--------|
| **Framework** | FastAPI | 0.104.1 | Rápido, type-safe, documentação automática |
| **Server** | Uvicorn | 0.24.0 | ASGI server de alta performance |
| **ORM** | SQLAlchemy | - | Versionamento DB (Alembic) |
| **Database** | PostgreSQL | 16 | Confiável, ACID, escalável |
| **Cache** | Redis | 7 | Subsecundo, distribuído |
| **Queue** | RabbitMQ | 3.13 | Resiliente, suporta prioritização |
| **Vector DB** | ChromaDB | 0.3.23 | Open source, Python nativo |
| **Storage** | Minio | latest | S3-compatible, local |
| **LLM** | Ollama | latest | Local, sem custos, privado |
| **TTS** | edge-tts | 6.1.12 | Grátis, múltiplas vozes |
| **Monitoring** | Prometheus | latest | Time-series, scrape jobs |
| **Dashboard** | Grafana | latest | Visualização, alertas |
| **Container** | Docker | - | Portabilidade, isolamento |
| **Orquestração** | Docker Compose | 1.29+ | Desenvolvimento, prototyping |

## 💰 Custo Estimado (Por Mês)

```
┌─────────────────────────────────────────────────────┐
│          DESENVOLVIMENTO LOCAL (Seu PC)              │
├─────────────────────────────────────────────────────┤
│ Harware:      GPU (já tem) + 20GB SSD               │
│ Ollama:       Grátis (open source)                  │
│ edge-tts:     Grátis (open source)                  │
│ Redis:        Grátis (open source)                  │
│ PostgreSQL:   Grátis (open source)                  │
│ Prometheus:   Grátis (open source)                  │
│ Grafana:      Grátis (open source)                  │
│                                                     │
│ CUSTO TOTAL:  R$ 0 (zero!)                          │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│    PRODUÇÃO (AWS t3.large - 100 podcasts/mês)       │
├─────────────────────────────────────────────────────┤
│ EC2 (2x t3.large):        ~$100                     │
│ RDS PostgreSQL:           ~$50                      │
│ ElastiCache Redis:        ~$30                      │
│ S3 (Armazenamento):       ~$10                      │
│ Ollama (inference):       ~$200 (com GPU)           │
│ Bandwidth:                ~$20                      │
│                                                     │
│ CUSTO TOTAL:  ~R$ 1.500-2.000/mês                   │
│ RECEITA (Pro):~R$ 5.000/mês (100 clientes @ R$50) │
│ MARGEM:       ~75%                                  │
└─────────────────────────────────────────────────────┘
```

## 🎓 Aprendizados Principais

1. **Microserviços não é sobre quantidade de serviços, é sobre limites claros**
2. **Docker Compose é perfeito para prototipagem rápida**
3. **Cache é mais importante que CPU para latência**
4. **Health checks salvam vidas (ou ao menos projetos)**
5. **Logging centralizado vale ouro em debugging**

---

**Versão**: 1.0  
**Data**: 9 de Fevereiro de 2026  
**Status**: ✅ Pronto para Teste
